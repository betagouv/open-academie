{
  "name": "open-academie-next",
  "version": "1.0.0",
  "repository": "git@github.com:sgmap/open-academie-next.git",
  "author": "Florian Delezenne <del.florian@gmail.com>",
  "homepage": "https://sgmap.github.io/open-academie-next",
  "dependencies": {
    "gh-pages": "^1.1.0",
    "next": "^4.2.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "next start -p $PORT",
    "build": "next build",
    "dev": "next",
    "postinstall": "yarn build",
    "deploy": "next build && next export -o docs && git checkout -- docs/.nojekyll && git add docs && git commit -m \"chore: regen site\" && git push origin master"
  }
}
